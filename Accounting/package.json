{
  "name": "reusable-resources-accounting",
  "version": "1.0.0",
  "description": "Small re-usable for finance/accounting on the web and can be used by e-commerce sites, MIS's e.t.c. Some of the components include but are not limited to; invoices, cheques, purchase order e.t.c",
  "main": "index.js",
  "scripts": {
    "prestart": "gulp sass && gulp minify",
    "start": "dotnet run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "finance",
    "accounting"
  ],
  "author": "Neon Clouds",
  "license": "ISC",
  "devDependencies": {
    "del": "^3.0.0",
    "gulp": "^4.0.0",
    "gulp-autoprefixer": "^6.0.0",
    "gulp-csso": "^3.0.1",
    "gulp-sass": "^4.0.2"
  }
}
